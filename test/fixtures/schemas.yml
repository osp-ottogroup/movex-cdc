# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# Only one schema defined here because several database support only one schema, additional schemas are defined in test setup if needed
one:
  id:   1
  name: <%= Trixx::Application.config.trixx_db_user %>

<% if Trixx::Application.config.trixx_db_type != 'SQLITE' %>
victim:
  id:   2
  name: <%= Trixx::Application.config.trixx_db_victim_user %>
<% end %>



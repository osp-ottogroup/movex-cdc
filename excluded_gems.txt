# This is a list of Gems that should be excluded from initialization
# In Gemfile they are marked with "gem name, require: false"
# After bundle install the Gem dir can be removed, but specifications and extensions must remain
benchmark
erb
io-console
irb
jar-dependencies
pp
prettyprint
prism
psych
reline

# rdoc 8.0.0 now depends on rbs which has a native extension
# rdoc < 8 does not have this dependency and is compatible with jRuby, so we use rdoc < 8.0
# Possibly another solutuion could be to use rdoc 8.0.0 and "gem 'rbs', '4.1.0.pre.2', platforms: :jruby"
rdoc, 7.2.0
